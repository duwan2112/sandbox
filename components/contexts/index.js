import UserContext from "./UserContext";
import {
  AlertContext,
  AlertContextProvider,
  AlertContextConsumer,
} from "./AlertContext";

const User = {
  UserContext,
};

const Alert = {
  AlertContext,
  AlertContextProvider,
  AlertContextConsumer,
};

export { User, Alert };
